/// <reference types="react" />
import { Option } from "../lib/interfaces";
interface ISelectListProps {
    focusIndex: number;
    options: Option[];
    onClick: Function;
}
declare const SelectList: ({ options, focusIndex, onClick }: ISelectListProps) => JSX.Element;
export default SelectList;
