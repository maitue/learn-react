export declare class JoinWithManySeparators {
    private _separators;
    constructor(separators: string[]);
    static join(arr: string[], separators: string[]): string;
    private _join;
    private _get;
}
